#include "tge/tge_canvas.h"

int tge_utf8_decode(const char *s, int *len)
{
    (void)s; (void)len;
    return -1;
}

int tge_utf8_char_width(uint32_t codepoint)
{
    (void)codepoint;
    return 1;
}
